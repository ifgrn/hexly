{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --env-file=.env run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "db:migration":"bun run src/db/migrations/migrations.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@libsql/client": "^0.17.0",
    "hono": "^4.11.3"
  }
}
